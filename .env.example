# Aaxion Docker Configuration

# Database path (inside container)
AAXION_DB_PATH=/data/.aaxion.db

# Port mapping (HOST:CONTAINER)
# Change the first number to use a different port on your host
# PORT=8080:8080

# User/Group ID for container (to avoid permission issues)
# Uncomment and set to your user ID if you encounter permission errors
# Run `id -u` and `id -g` to get your IDs
# USER_ID=1000
# GROUP_ID=1000
